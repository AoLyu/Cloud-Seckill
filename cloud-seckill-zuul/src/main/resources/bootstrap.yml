server:
  port: 8092

spring:
  application:
    name: cloud-seckill-zuul
  management:
    security:
      enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8091/eureka/
    registry-fetch-interval-seconds: 10

zuul:
  ignoredServices: '*'
  routes:
    cloud-seckill-user: /user/**
    cloud-seckill-item: /item/**
    cloud-seckill-order: /order/**
    cloud-seckill-auth: /auth/**
  sensitive-headers:
